# Copie para .env e preencha. O .env é ignorado pelo git — NUNCA commitar credenciais.
# Console Google:  https://console.cloud.google.com/apis/credentials
# Console Discord: https://discord.com/developers/applications

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Webhook do canal de novidades (RF-NOV-11) — Server Settings > Integrations > Webhooks
DISCORD_WEBHOOK_URL=

# DM de notificações via bot (RF-NOT-04) — Bot > Token, na aplicação do Discord
DISCORD_BOT_TOKEN=

# Web Push VAPID (RF-NOT-02). Gere um par com: WebPush.generate_key
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=

# Pagamento Mercado Pago (RF-LOJ-12/RN-12) — Checkout Pro. Credenciais em
# https://www.mercadopago.com.br/developers > Suas integrações. Use as de TESTE
# (sandbox) em dev. O webhook secret valida a assinatura das notificações.
MERCADO_PAGO_ACCESS_TOKEN=
MERCADO_PAGO_PUBLIC_KEY=
MERCADO_PAGO_WEBHOOK_SECRET=
# Host público para as back_urls/notification_url do checkout (mesmo do mailer)
APP_HOST=

# Melhor Envio (RF-LOJ-11) — frete/etiqueta/rastreio. Token OAuth do app em
# https://melhorenvio.com.br (ou sandbox.melhorenvio.com.br). SANDBOX=false só em
# produção. Cadastro do remetente = CPF de um responsável da liga (§8; a liga não
# tem CNPJ). Sem TOKEN, a cotação/etiqueta respondem "indisponível".
MELHOR_ENVIO_TOKEN=
MELHOR_ENVIO_SANDBOX=true
MELHOR_ENVIO_CEP_ORIGEM=
MELHOR_ENVIO_CPF_REMETENTE=
MELHOR_ENVIO_NOME_REMETENTE=
MELHOR_ENVIO_LOGRADOURO_REMETENTE=
MELHOR_ENVIO_NUMERO_REMETENTE=
MELHOR_ENVIO_CIDADE_REMETENTE=
MELHOR_ENVIO_UF_REMETENTE=

# ===== Produção (feature/deploy-producao) — injetadas pelo Kamal =====
# Em dev os defaults do docker-compose bastam; abaixo é o que produção espera.

# Banco (o acessório Postgres do Kamal usa POSTGRES_USER/PASSWORD)
DB_HOST=
DB_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=

# E-mail SMTP (recuperação de senha do Devise, RF-AUT-04). address/port/user/pass
SMTP_ADDRESS=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=

# Cloudflare R2 — Active Storage em produção (RNF-13). endpoint =
# https://<account_id>.r2.cloudflarestorage.com
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_ENDPOINT=
R2_BUCKET=

# Registro de imagens (ghcr.io) — PAT com write:packages para o Kamal
KAMAL_REGISTRY_USERNAME=
KAMAL_REGISTRY_PASSWORD=
